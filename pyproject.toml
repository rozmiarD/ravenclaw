[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ravenclaw"
version = "0.10.0"
description = "Governance-first security research runtime"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Krzysztof Probola" }]
dependencies = [
  "PyYAML>=6,<7",
  "sclite @ git+https://github.com/rozmiarD/SCLite.git@43dae49b44602da76611fb42cd0b10aac3b3ae3f",
  "govengine @ git+https://github.com/rozmiarD/GovEngine.git@de8e53bcfddadb9f64d51b998aee5e9657d97591",
]

[project.optional-dependencies]
logdash = [
  "Flask>=3,<4",
]
dev = [
  "pytest>=8,<9",
  "Flask>=3,<4",
  "PyYAML>=6,<7",
]

[tool.setuptools]
packages = []
py-modules = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["engine/tests", "engine/planer/tests", "tests"]

